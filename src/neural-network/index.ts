import {computeNetwork} from "./neural-network"
import {weightNetworkWith, randomWeightNetwork} from "./weights"

const NeuralNetwork = {
  computeNetwork,
  weightNetworkWith,
  randomWeightNetwork,
}

export default NeuralNetwork
